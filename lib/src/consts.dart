/// @docImport 'package:flutter/material.dart';
library;

import 'dart:math' as math;
import 'package:flutter/rendering.dart';

const maxRadians = math.pi * 2;

const double topCenter = -(math.pi / 2);

/// default constraints for [CircularProgressIndicator] Material 3 y2023 and Material 2
const BoxConstraints circularDefaultConstraints = BoxConstraints(minWidth: 36.0, minHeight: 36.0);